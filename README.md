# Analogical Learning-Based Wireless Localization (ALLoc)

Zirui Chen, Hongning Ruan, Zhaoyang Zhang, Ziqing Xing, Ridong Li, Zhaohui Yang and Mérouane Debbah

------

This repository is the official implementation of paper [Analogical Learning for Cross-Scenario Generalization: Framework and Application to Intelligent Localization](https://arxiv.org/abs/2504.08811).

**Note: The latest version of this repository is a reimplementation of the original version, featuring clearer logic and improved efficiency. The original implementation has been tagged as `v1` and can be accessed [here](https://github.com/ziruichen-research/ALLoc/tree/v1).**

## Overview of This Work

This work proposes a new framework called ***Analogical Learning*** (AL). It enables direct adaptation to diverse scattering environments and system configurations without requiring additional training, offering a new technical system with cross-scenario generalization for intelligent wireless localization and various other wireless AI tasks. This breakthrough stems from a key insight into the relativity of numerical feature representations: isolated numerical values generally cannot convey complete meaning unless combined with corresponding reference frame information. The lack of ability to aware reference system information restricts existing wireless AI methods (e.g., data-to-label learning) to generalizing only within specific underlying reference systems, resulting in poor cross-scenario reusability and high sensitivity to dynamic environmental changes.

Therefore, to complement reference frame information, AL first embeds known data-label pairs from the current scenario as reference objects in the training and inference process, resembling the in-context learning (ICL) in the expression form. The most notable innovation lies in the development of ***Mateformer***, inspired by the insight of relativity. This network designs an advanced multi-layer analogical mechanism, enabling the interpretation of feature information through multi-space relativity and fully leveraging reference frame information. The application of AL to wireless localization demonstrates its superiority: it achieves **state-of-the-art accuracy**, **stable transferability**, and **seamless adaptation to new scenarios without any tuning**, outperforming conventional methods by nearly two orders of magnitude in precision. This expansion of wireless intelligence in multi-scenario dimension marks an important step toward [wireless big AI models](https://ieeexplore.ieee.org/document/10579546).

## Requirements

The code in this repository requires the following dependencies:

```text
calflops==0.3.2
h5py==3.11.0
numpy==1.24.3
PyYAML==6.0.2
torch==2.4.1+cu118
tqdm==4.67.1
```

## Datasets

All datasets used in our experiments have been organized into HDF5 format and can be downloaded via the following links.

*Google Driven*: https://drive.google.com/drive/folders/183kxLUp7nJJxgZ8zQ7lJT80xRmO5HrZT?usp=drive_link

*Baidu Netdisk*: https://pan.baidu.com/s/1IP47KTdbG0E4utnpKERfVA?pwd=3xbu

After the download is complete, please place all the compressed files in the directory of this repository, then run the following commands.

```shell
cat dataset_deepmimo.tar.gz.* | tar xvzf -
cat dataset_deepmimo_rain.tar.gz.* | tar xvzf -
cat dataset_deepmimo_road.tar.gz.* | tar xvzf -
cat dataset_dichasus.tar.gz.* | tar xvzf -
cat dataset_sionna.tar.gz.* | tar xvzf -
```

This will create the following subdirectories: `dataset_deepmimo/`, `dataset_deepmimo_rain/`, `dataset_deepmimo_road/`, `dataset_dichasus/`, and `dataset_sionna/`.

|Subdirectory|Experimental scenarios|Data size (before expansion)|Description|
|-|-|-|-|
|`dataset_deepmimo/`|O1, O1B, MO1|2.5GB|Based on [DeepMIMO V1](https://arxiv.org/abs/1902.06435)|
|`dataset_deepmimo_rain/`|RO1|4.7GB|Generated by adding rain attenuation to the O1 dataset according to [ITU-R P.838-3](https://www.itu.int/rec/r-rec-p.838-3-200503-i)|
|`dataset_deepmimo_road/`|O2|16GB|Based on [DeepMIMO V2](https://www.deepmimo.net/)|
|`dataset_dichasus/`|DICHASUS-ca0x|867MB|Based on [DICHASUS-ca0x](https://dichasus.inue.uni-stuttgart.de/datasets/data/dichasus-ca0x/)|
|`dataset_sionna/`|Realistic urban scenarios|4.2GB|Generated by [Sionna RT](https://ieeexplore.ieee.org/document/10465179/) on 3D urban scenes retrieved from [OpenStreetMap](https://www.openstreetmap.org/)|

As shown in the table above, these data files require tens of GB of storage space. To improve training efficiency, we provide the *gen_seq_datasets* parameter in the code; when it is set to True, each training dataset will be expanded to a much larger size during the ALLoc training process (specifically, to *max_len_nei* + 1 times the original size, which in our experiments is 65 times the original size). In this case, the actual storage space required for each training run can range from hundreds of GB to more than 1 TB. We recommend reserving sufficient storage space before starting each training run if setting *gen_seq_datasets* to True. For more details, please refer to the description of the *gen_seq_datasets* parameter in `README.doc.md`.

### DICHASUS-ca0x

For the DICHASUS-ca0x dataset, we have provided in the `tools/` subdirectory the code that we used for dataset pre-processing, which additionally requires the following dependencies:

```text
scipy==1.18.0
tensorflow==2.13.0
```

To run the code, please download the raw data files from the official website:

https://dichasus.inue.uni-stuttgart.de/datasets/data/dichasus-ca0x/

Then move the data files into the `tools/` subdirectory and run the following commands.

```shell
cd tools
python prepare_dataset_ca01.py
python prepare_dataset_ca02.py
cd ..
cp dataset_dichasus/ca01/train.h5 dataset_dichasus/ca02/train.h5
```

This will create the `dataset_dichasus/` subdirectory that contains all the pre-processed data files.

## Training and Testing

All experiments run the same code, `main.py`, but with different configuration parameters. Specifically, each experiment is configured using a source configuration file specified via the `--conf` option; additionally, parameters from the source configuration file can be temporarily modified for the current experiment via the `--mod` option.

Each experiment is identified by the *exp_dir* and *task* parameters. When a new experiment is launched, a copy of the source configuration file will be created in the *exp_dir* directory and renamed as `train.yaml` (if *task* is 'train') or `test.yaml` (if *task* is 'test'). Parameter modifications made via the `--mod` option will be applied to the copied configuration file, while the source configuration file remains unchanged.

Configuration file templates for both training and testing are provided in the `conf/` directory. Therefore, a convenient way to configure an experiment is to specify a template as the source configuration file using the `--conf` option, and modifying certain parameters in the copied configuration file using the `--mod` option.

All pretrained model files and main configuration files used in our experiments have been packaged into a single compressed file (approximately 1.04 GB) and can be downloaded via the following link.

*Google Driven*: https://drive.google.com/file/d/1nlET4DmBgC82Qjx_9X3H8V8BN_0gLDwd/view?usp=drive_link

Please place the compressed file in the directory of this repository and use the following command to extract it, which will create the `exp/` subdirectory.

```shell
tar xvzf exp.tar.gz
```

The configuration files in the `exp/` subdirectory can be used directly for experiment runs via the `--conf` option, while the model files can be used directly for testing without the need for retraining.

### Example Usage

The following example trains AD_CNN in DeepMIMO O1 scenario, making use of the `conf/train.yaml` template and modifying the following parameters: *exp_dir*, *data_dirs*, *scheme*, *num_train_data*, and *init_lr*.

```shell
python main.py --conf=./conf/train.yaml --mod exp_dir=./exp/o1_40k/ad_cnn/ \
    data_dirs=[./dataset_deepmimo/o1/] \
    scheme=ad_cnn num_train_data=40000 init_lr=0.001
```

If the *exp_dir* and *task* parameters match those of a previous experiment, the code will use that experiment's existing configuration file and ignore any new configurations specified via the `--conf` or `--mod` options. This is particularly useful for resuming training from the latest checkpoint. 

```shell
python main.py --conf=./exp/o1_40k/ad_cnn/train.yaml
# Equivalent to
python main.py --conf=./conf/train.yaml --mod exp_dir=./exp/o1_40k/ad_cnn/
```

For more usage examples and detailed descriptions of the configuration parameters, please refer to `README.doc.md`.

In our experiments, all models can be trained using a single GPU with 32GB of memory (both the model and data are based on float32). If the device has insufficient memory, the *batch_size* parameter can be appropriately reduced. 

## Citation

If you find this work useful in your research, please consider citing us:

```bibtex
@article{chen2025AL,
  title={Analogical Learning for Cross-Scenario Generalization: Framework and Application to Intelligent Localization},
  author={Chen, Zirui and Ruan, Hongning and Zhang, Zhaoyang and Xing, Ziqing and Li, Ridong and Yang, Zhaohui and Debbah, Mérouane},
  journal={arXiv preprint arXiv:2504.08811},
  year={2025}
}
```